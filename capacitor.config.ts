import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.carejrz.app',
  appName: 'careapp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
